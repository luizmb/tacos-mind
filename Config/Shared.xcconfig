// Checked in. DEVELOPMENT_TEAM defaults to empty so a fresh clone (and CI, which builds
// with CODE_SIGNING_ALLOWED=NO anyway) never needs a real team id. #include? is optional —
// it silently no-ops when Local.xcconfig doesn't exist. See Local.xcconfig.example.
DEVELOPMENT_TEAM =
#include? "Local.xcconfig"
